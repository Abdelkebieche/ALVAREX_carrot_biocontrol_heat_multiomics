# Data policy

Large raw and processed datasets are intentionally **not committed**. The repository contains code, metadata needed for design interpretation, and input schemas. Provide local data through the variables in `config/config.R`.

Do not commit FASTQ, BAM, STAR indices, raw LC-MS exports, private paths, or institutional credentials.
