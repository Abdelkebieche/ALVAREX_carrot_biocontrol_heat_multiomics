# Software environment

`software_versions.tsv` records versions explicitly stated in the manuscript. Versions not stated are left as `not reported` rather than guessed. For a final archival release, capture `sessionInfo()` from the exact R environment used to regenerate the figures.
