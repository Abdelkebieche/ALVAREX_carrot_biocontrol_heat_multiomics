# Configuration

`config.R` contains only generic, repository-relative defaults. It deliberately contains no institutional cluster, account, partition, email address, or private filesystem path.

For a local run, either set `CARROT_REPO_ROOT` and the input environment variables listed in `config.R`, or edit a private copy that is **not committed**.
